"""Integration tests for the ingest pipeline (requires running Kafka + MinIO)."""

import pytest


@pytest.mark.integration
def test_placeholder():
    """Placeholder — pipeline integration tests require live Kafka and MinIO."""
    pass
